# Optional GitHub Personal Access Token used as a dev fallback.
# When set, this token is sent with every GraphQL request, overriding
# whatever the user has saved in the in-app token dialog.
# Recommended PAT scopes (classic): public_repo, read:user
VITE_GITHUB_TOKEN=
